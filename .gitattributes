# Force LF on checkout for shell scripts (bash breaks on CRLF)
*.sh text eol=lf
*.py text eol=lf

# PowerShell works with either, but CRLF is conventional on Windows
*.ps1 text eol=crlf

# Everything else: normalize to LF in the repo, native on checkout
* text=auto

# Markdown: LF (editorconfig already enforces this)
*.md text eol=lf

# Config files: LF
*.yml text eol=lf
*.yaml text eol=lf
*.json text eol=lf
