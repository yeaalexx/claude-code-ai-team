<!-- AUTO-CREATED by Claude Code during project bootstrap.
     Standard HTTP headers that all services must handle.
     Claude will populate project-specific values during bootstrap. -->

# Shared Header Rules

> Standard headers that all services MUST accept, forward, or set.
> Every inter-service call must include these headers.

## Standard Headers

| Header | Required | Source | Rules |
|--------|----------|--------|-------|
| `Authorization` | <!-- Yes/No --> | <!-- e.g., Client → Gateway → Services --> | <!-- e.g., Bearer JWT, must forward verbatim, never strip --> |
| `X-Tenant-ID` | <!-- Yes/No --> | <!-- e.g., Extracted from JWT claim or set by gateway --> | <!-- e.g., UUID format, must match JWT tenant_id claim --> |
| `X-Request-ID` | <!-- Yes/No --> | <!-- e.g., Generated by gateway if missing --> | <!-- e.g., UUID-v4, propagate through all downstream calls for tracing --> |
| `Content-Type` | <!-- Yes/No --> | <!-- e.g., Set by caller --> | <!-- e.g., application/json for REST, application/protobuf for gRPC --> |

<!-- Add project-specific headers below as needed. -->

## Forwarding Rules

- **Gateway to services:** All standard headers listed above MUST be forwarded.
- **Service to service:** When one service calls another, it MUST propagate `Authorization`, `X-Tenant-ID`, and `X-Request-ID`.
- **Never strip:** Proxies (nginx, API gateway) must never strip `Authorization` or tenant headers.

## Notes

<!-- Add project-specific header notes here, e.g.:
     - "nginx proxy_pass must include: proxy_set_header Authorization $http_authorization;"
     - "X-Tenant-ID is validated against JWT claim at the service level, not the gateway"
-->
